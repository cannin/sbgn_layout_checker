[project]
name = "sbgn-layout-checker"
version = "0.1.1"
description = "Check SBGN layouts for arc crossings and arc-node overlaps."
requires-python = ">=3.14"
dependencies = [
    "render-sbgn-py @ git+https://github.com/cannin/render_sbgn.git@f2985994e867f7c94d46c52284b555f02ea3af76#subdirectory=python",
]

[project.optional-dependencies]
test = ["pytest>=9.0.2", "ruff>=0.15.5"]

[project.scripts]
sbgn-layout-checker-py = "sbgn_layout_checker.analyze_sbgn_layout:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.12.11",
]

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["sbgn_layout_checker"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
pythonpath = ["."]
